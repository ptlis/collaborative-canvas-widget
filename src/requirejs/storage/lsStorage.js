/*jshint jquery:true */


define( ['jquery'],
        function($) {
            'use strict';

            var lsStorage   = {};

            lsStorage.init = function() {

            };

            lsStorage.uiUpdate = function() {

            };

            return lsStorage;
        }
);
